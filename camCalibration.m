%camera calibration this uses the built in function for camera calibration
%make a file called pics and put all the images from picturecap in that
%then run this the size and units are for the board i used if you have a
%different on then just measure and replace
cameraCalibrator('pics',1.338,'cm')